# Contributing

Keep changes focused on the documented HookReplay behavior. Add a regression test for behavior changes, update public API baselines for API changes, and run the local validation commands in AGENTS.md.

Do not add credentials, real customer data, generated build output, or local telemetry configuration to commits.
